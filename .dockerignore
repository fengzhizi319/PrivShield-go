# =============================================================================
# Docker 构建上下文排除列表
# Docker build context exclusion list
#
# 说明：以下文件/目录不会被复制到镜像中，减小镜像体积并避免泄露敏感信息
# =============================================================================

# ----------------------------------------------------------------------------
# Python 虚拟环境与缓存
# Python virtualenv and cache
# ----------------------------------------------------------------------------
.venv
venv/
env/
.pytest_cache
__pycache__
*.pyc
*.pyo
*.pyd
.hypothesis
.benchmarks
.coverage
*.egg-info

# ----------------------------------------------------------------------------
# IDE 与编辑器配置
# IDE and editor configuration
# ----------------------------------------------------------------------------
.git
.gitignore
.github
.idea
.vscode
*.swp
*.swo

# ----------------------------------------------------------------------------
# 本地配置与日志（避免泄露）
# Local config and logs (avoid leakage)
# ----------------------------------------------------------------------------
.bin
.local
.logs/
*.log
.env
.env.*
!.env.example

# ----------------------------------------------------------------------------
# 模型权重与训练产物（体积大，按需挂载）
# Model weights and training artifacts (large, mount as needed)
# ----------------------------------------------------------------------------
.models/
models/
basemodels/
services/privacy-engine/model-training/
**/model-training/
llmlora/

# ----------------------------------------------------------------------------
# 前端构建产物（由多阶段构建处理）
# Frontend build artifacts (handled by multi-stage build)
# ----------------------------------------------------------------------------
console/web/node_modules
console/web/dist
console/web/.vite

# ----------------------------------------------------------------------------
# 证书与密钥（安全敏感）
# Certificates and keys (security sensitive)
# ----------------------------------------------------------------------------
certs/*.key
certs/*.crt
certs/*.pem
*.pem
*.key

# ----------------------------------------------------------------------------
# 临时文件与压缩包
# Temp files and archives
# ----------------------------------------------------------------------------
tmp/
*.tar.gz
*.zip
*.whl

# ----------------------------------------------------------------------------
# 文档与站点（运行时不需要）
# Documentation and site (not needed at runtime)
# ----------------------------------------------------------------------------
docs/*
!docs/standard/
!docs/standard/*.md
site/
*.md
!README.md
!docs/standard/*.md

# ----------------------------------------------------------------------------
# 测试与开发配置
# Tests and development config
# ----------------------------------------------------------------------------
tests/
scratch/
.pytest_cache
mkdocs.yml
.pre-commit-config.yaml

# ----------------------------------------------------------------------------
# 部署配置（避免循环）
# Deployment config (avoid circularity)
# ----------------------------------------------------------------------------
deploy/
Makefile
